# Automated gpuagent bump to ROCm/gpu-agent main @06c28090845b

- **Date:** 2026-08-11
- **Author:** Bhanu Kiran Atturu
- **Related PR(s):** (this PR)
- **Related issue(s) / JIRA:** N/A — dependency bump

## Context

ROCm/gpu-agent `main` advanced past the currently pinned `GPUAGENT_COMMIT`.
Bump the pin so the exporter builds against current upstream gpuagent.

## Approach

- Bump `GPUAGENT_COMMIT` from `84bc85f69e41c9b386dd82afa842987b3e549b00` to
  `06c28090845b86f385625da51e45c3fc2b479f75` in both pin locations:
  - `Makefile`
  - `docker/Dockerfile.exporter-release`

### Alternatives considered

N/A — mechanical dependency bump, no design choice involved.

## Scope

- **In scope:** `GPUAGENT_COMMIT` pin.
- **Out of scope:** any other asset (amdsmi, ROCm version, patches). If the new
  gpuagent commit needs a vendor patch refresh or breaks the build, a human must
  follow up — this only updates the version pin.

## Validation

- **Required before merge:** `make docker` (or the `/builder` skill) builds clean
  at the new commit, and a smoke test confirms gpuagent/gpuctl still function.

## Risks and rollback

- **Known risks:** a gpuagent commit bump can change metric behavior, add new
  dependencies, or require a vendor patch refresh. Do not merge without a
  successful build.
- **Rollback:** revert this PR; `GPUAGENT_COMMIT` returns to the prior pinned value.
